language: r
# cache: packages

# Add devtools to force install so works on r-devel
r_packages:
  - devtools

matrix:
  include:
    - os: linux
      r: devel

    - os: linux
      r: release

    - os: osx
      r: devel

    - os: osx
      r: release

  allow_failures:
    - os: osx
      r: devel

notifications:
  email:
    on_success: change
    on_failure: change
